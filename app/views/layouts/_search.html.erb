<div class="row">
  <div class="col">
    <% if user_signed_in? %>
      <div class="search_form">
        <%= form_with url: search_path, method: :get, local: true do |f| %>
          <%= f.text_field :word, placeholder: '検索ワードを入力してください' %>
          <%= f.submit "検索", class: "btn btn-sm btn-primary" %>
        <% end %>
        ※ユーザー検索は先頭に@を付けてください
      </div>
    <% end %>
  </div>
</div>
